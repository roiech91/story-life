
# Frontend (Vite + React + Tailwind)

## Setup
```bash
npm i
npm run dev
```

> ודא שה-API רץ על `http://localhost:8000` או הגדר משתנה סביבה:
```bash
echo "VITE_API_BASE=http://localhost:8000" > .env.local
```

## Build
```bash
npm run build && npm run preview
```
